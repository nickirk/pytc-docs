# Sphinx toolchain (installed in CI via GitHub Actions)
sphinx>=7.0
furo
myst-parser
sphinx-copybutton
sphinx-autodoc-typehints

# The workflow installs pytc-qc separately with --no-deps.  Missing heavy
# runtime dependencies are mocked by conf.py so the docs build needs no GPU
# stack.
